/*** Calculate the factorial of n using recursion. Assumption: n >= 1. ***/

public class Solution {
  public long factorial(int n) {
    // Time: O(n), Space: O(n) ???
    // n! = (n - 1)! * n
    if (n == 1) return 1;
    return factorial(n - 1) * n;
  }
}
