class Solution {
    // Time, Space: O(1)
    public int addDigits(int num) {
        if (num == 0) {
            return num;
        } else if (num % 9 == 0) {
            return 9;
        } else {
            return num % 9;
        }
    }
}
