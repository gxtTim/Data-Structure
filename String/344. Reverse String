class Solution {

    // Be careful when the length of String is overflowed. The max length of String is Integer.MAX_VALUE
    // But char is array, which can be enlarged forever
    // Time: O(n), Space: O(1)
    
    public String reverseString(String s) {
        if (s == null || s.length() <= 1) {
            return s;
        }
        char[]ch = s.toCharArray();
        char[]res = new char[ch.length];
        
        // Be careful when using loops, if it is loop(loop), the ourput was "hhhh", which is wrong
        
        for (int i = 0; i < res.length; i++) {
            res[i] = ch[ch.length - 1 - i];
        }
        return new String(res);
    }
}
